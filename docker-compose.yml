version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      POSTGRES_DB: ${POSTGRES_DB:-photoclub}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  minio:
    image: minio/minio
    restart: always
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  # api-server:
  #   build:
  #     context: ./api-server
  #     dockerfile: Dockerfile
  #   restart: always
  #   depends_on:
  #     - postgres
  #     - redis
  #     - minio
  #   environment:
  #     DATABASE_URL: postgres://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/${POSTGRES_DB:-photoclub}
  #     REDIS_URL: redis://redis:6379
  #     MINIO_ENDPOINT: http://minio:9000
  #     MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
  #     MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
  #     MINIO_BUCKET: ${MINIO_BUCKET:-photos}
  #   ports:
  #     - "3000:3000"

  # worker:
  #   build:
  #     context: ./api-server
  #     dockerfile: Dockerfile.worker
  #   restart: always
  #   depends_on:
  #     - redis
  #     - minio
  #     - postgres
  #   environment:
  #     DATABASE_URL: postgres://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-postgres}@postgres:5432/${POSTGRES_DB:-photoclub}
  #     REDIS_URL: redis://redis:6379
  #     MINIO_ENDPOINT: http://minio:9000
  #     MINIO_ACCESS_KEY: ${MINIO_ROOT_USER:-minioadmin}
  #     MINIO_SECRET_KEY: ${MINIO_ROOT_PASSWORD:-minioadmin}
  #     MINIO_BUCKET: ${MINIO_BUCKET:-photos}

volumes:
  postgres_data:
  redis_data:
  minio_data:
